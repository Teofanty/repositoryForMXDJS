/* ==================
 脚本类型:  传送门    
 版权：游戏盒团队     
 联系扣扣：297870163    609654666
 =====================
 */
 function enter(pi) {
    var qq = 3719;
    if (pi.isQuestActive(qq)) {
	pi.forceCompleteQuest(qq);
	pi.playerMessage("任务完成");
    }
    qq = 3724;
    if (pi.isQuestActive(qq)) {
	pi.forceCompleteQuest(qq);
	pi.playerMessage("任务完成");
    }
    qq = 3730;
    if (pi.isQuestActive(qq)) {
	pi.forceCompleteQuest(qq);
	pi.playerMessage("任务完成");
    }
    qq = 3736;
    if (pi.isQuestActive(qq)) {
	pi.forceCompleteQuest(qq);
	pi.playerMessage("任务完成");
    }
    qq = 3742;
    if (pi.isQuestActive(qq)) {
	pi.forceCompleteQuest(qq);
	pi.playerMessage("任务完成");
    }
    qq = 3748;
    if (pi.isQuestActive(qq)) {
	pi.forceCompleteQuest(qq);
	pi.playerMessage("任务完成");
    }
}